import { keymirror as km } from '../util';
let result;
export const {
	// 获取所有得配置
	FETCH_CONFIG,
	FETCH_CON_DATA,
} = (result = km([
	// 获取所有得配置
	'FETCH_CONFIG',
	'FETCH_CON_DATA',
]));
export default result;
