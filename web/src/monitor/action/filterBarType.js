import {keymirror as km} from "../util";
let result;
export const {
	MONITOR_FILTER_TYPE,
	MONITOR_FILTER_CONDITION,
	HIDDEN_DATA_URL,
} = result = km([
	// 监控过滤类型
	"MONITOR_FILTER_TYPE",
	// 监控过滤url
	"MONITOR_FILTER_CONDITION",
	// 是否隐藏dataType
	"HIDDEN_DATA_URL",
]);
export default result;
