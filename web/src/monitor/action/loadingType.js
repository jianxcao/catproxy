import { keymirror as km } from '../util';
let result;
export const {
	// 页面加载
	LOADING_PAGE,
	// 加载请求内容
	LOADING_CON_DATA,
} = (result = km([
	// 获取所有得配置
	'LOADING_PAGE',
	'LOADING_CON_DATA',
]));
export default result;
